const imagem = document.querySelection('img');
const botao = document.querySelection('button');
const nomeDoPersonagem = document.querySelection('#nome');
const especie = document.querySelection('#especie');
const status = document.querySelection('#status');